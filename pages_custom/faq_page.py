import streamlit as st

def faq_page():
    st.title("❓ FAQ — Как считаются стоимость и зарплата игрока")

    st.header("📌 Формула трансферной стоимости")
    st.markdown("""
    **Трансферная стоимость** игрока рассчитывается на основе нескольких показателей:

    ```
    transfer_price = (skill_score + mmr_score + media_score)
                     × role_modifier
                     × behavior_coeff
                     × versatility_bonus
                     × base_rate
    ```

    🔹 **skill_score** — комплексная оценка навыков игрока:  
    основывается на **KDA**, **GPM/XPM**, **винрейте**, **участии в командных убийствах** и **разнообразии героев**.

    🔹 **mmr_score** — вклад рейтинга MMR:  
    ```
    mmr_score = min(max((MMR - 4000) / 200 × 1.5, 0), 15)
    ```

    🔹 **media_score** — медийная активность (по умолчанию 3), задаётся вручную.

    🔹 **role_modifier** — модификатор в зависимости от роли игрока  
    (например, core может быть ×1.2, support — ×1.0 и т.д.).

    🔹 **behavior_coeff** — поведенческий коэффициент (например, 1.0 при хорошем поведении, < 1.0 при частых репортах).

    🔹 **versatility_bonus** — коэффициент универсальности:  
    повышается, если игрок играет на большом числе героев.

    🔹 **base_rate** — базовая ставка стоимости, по умолчанию = 1000.

    ---
    **Пример:**  
    Если:
    - skill_score = 32.5  
    - mmr = 6800 → mmr_score ≈ 13.5  
    - media_score = 3  
    - role_modifier = 1.1  
    - behavior_coeff = 0.95  
    - versatility_bonus = 1.1  

    Тогда:  
    ```
    total_score = (32.5 + 13.5 + 3) × 1.1 × 0.95 × 1.1 × 1000 = ~$56,573
    ```

    """)

    st.header("💼 Формула расчёта зарплаты игрока")
    st.markdown("""
    **Зарплата** игрока рассчитывается как процент от его трансферной стоимости,
    с учётом роли, поведения и медийности:

    ```
    salary = transfer_price × base_percent × role_modifier × behavior_coeff + media_score × 100
    ```

    🔹 **base_percent** зависит от MMR:
    | MMR | base_percent |
    |-----|---------------|
    | ≥ 7000 | 0.034 |
    | ≥ 6000 | 0.026 |
    | ≥ 5000 | 0.022 |
    | ≥ 4000 | 0.018 |
    | ≥ 3000 | 0.011 |
    | < 3000 | 0.010 |

    🔹 **role_modifier** — множитель от роли (например, carry > support).  
    🔹 **behavior_coeff** — понижается, если игрок часто получает жалобы.  
    🔹 **media_score × 100** — бонус за медийность (подкасты, соцсети, стримы и т.д.).

    ---
    **Пример:**  
    Если:
    - transfer_price = 56573  
    - mmr = 6800 → base_percent = 0.026  
    - role_modifier = 1.1  
    - behavior_coeff = 0.95  
    - media_score = 3  

    Тогда:
    ```
    base_salary = 56573 × 0.026 ≈ 1470.9  
    final_salary = 1470.9 × 1.1 × 0.95 + 3 × 100 ≈ $1,636.5
    ```

    """)

    st.info("📌 Все значения формул могут быть откорректированы администрацией проекта при изменении мета-игры или API.")
